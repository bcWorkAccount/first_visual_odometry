/**
* This file is part of FVO.
* FileState:  the helper class of file system.
 * judge the file or directory state
 *
 * Author: Arhtur.Chen
 * Email: shihezichen@live.cn
 * Created: 27th Jan, 2018
*/

#include "fvo/FileState.h"

namespace fvo {

    // judge whether the directory exists or not
    bool FileState::isDirExist(std::string path) {
        // Todo
        LOG(WARNING) << "Todo: FileState::isDirExist()" <<  std::endl;
        return true;
    }

    // judge whether the file exists or not
    bool FileState::isFileExist(std::string path){
        // Todo
        LOG(WARNING) << "Todo: FileState::isFileExist()" <<  std::endl;
        return true;
    }

} // end of namespace